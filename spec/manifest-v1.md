# Eval Run Manifest v1

Fields: `run_id`, `timestamp`, `code_sha`, `env`, `rubric_ref`, `judge_card_ref`, `contamination_report_ref`, `dataset_card_refs`, `results_digest`, and optional `signature`. Signing is optional and can use sigstore externally.
